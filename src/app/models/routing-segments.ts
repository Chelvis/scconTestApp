export interface RoutingSegments {
    search: string;
    data: string;
    register: string;
}

export const RoutingSegments: RoutingSegments = {
    search: 'busca',
    data: 'dados',
    register: 'cadastro'
};
